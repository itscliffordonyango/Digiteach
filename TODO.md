# TODO
- [x] Understand original static `home.html` and current React component structure.
- [x] Decide asset strategy: use existing top-level `assets/` by copying into `frontend/public/assets`.
- [x] Create `frontend/public/assets` and copy all asset folders/files there.
- [x] Verify Vite dev server and runtime resolution for key image/CSS asset URLs.
- [x] Remove unnecessary/redundant project files: deleted top-level duplicate `assets/` after copying into `frontend/public/assets/.` (reported previously)
- [x] Ensure React page output loads the same full sections as `home.html` (Access/Confidence/Why/Help) so page matches original.

- [x] Cross-reference and fix any incorrect link paths in React components/CSS against `home.html`.

- [ ] Improve Footer (optional placeholder replacement) if required by matching original `home.html`.
- [ ] Run `frontend` build and basic runtime checks after final edits.

